version: 2

models:
  - name: v_stg_bpm_auth_nhan_vien
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_auth_to_chuc_don_vi
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_giao_dich
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_giao_dich_link
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_giao_dich_sla
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_giao_dich_tsbd
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_khach_hang
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_pdtd_nhom_giao_dich
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_pdtd_san_pham
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_qlns_nguoi_lao_dong_info
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"


  - name: v_stg_bpm_tdcn_tai_san_bao_dam
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_tdcn_thong_tin_chung
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_tdcn_xl_gdtd
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_tdtc_nghiep_vu_xl_gdtd
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_tdtc_thong_tin_chung
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_tdtc_thong_tin_to_trinh
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_tht_nhom_chinh_sach_the
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_tsbd_giaodich_chinh
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_tsbd_giaodich_chitiet
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_tsbd_giaodich_lich_su_v2
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_tsbd_giaodich_taisan_chusohuu
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_tsbd_tai_san
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_xl_ct_han_muc
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_xl_ct_lich_su
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_xl_ksgn_chung
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_xl_ksgn_ct
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_xl_pdtd_link
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_pdtd_tsbd_link
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_giao_dich_the
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_h_pdtd_gdich_ctiet_pduyet
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_h_pdtd_gdich_lsu_pduyet
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_khach_hang_ca_nhan
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_khach_hang_doanh_nghiep
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_lich_su_giao_dich
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_pdtd_giao_dich_tin_dung
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_tc_stk_lich_su
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_tc_stk_thong_tin_tong_hop
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_tcstk_thong_tin_bao_dam
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_tcstk_thong_tin_chung
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_tcstk_thong_tin_giao_dich
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_tdcn_chi_tiet_san_pham
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_tdcn_khoan_cap_td_the
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_tdcn_nguoi_dong_vay
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_tdcn_sp_nhu_cau_td
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_tdcn_thu_nhap_tra_no
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key

  - name: v_stg_bpm_tsbd_chu_so_huu
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"

  - name: v_stg_bpm_ttd_giao_dich_igen
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
