version: 2

models:
  - name: v_stg_ocbchannel_admin_command_queue
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
  - name: v_stg_ocbchannel_apportion_trans_batch_details
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key
  - name: v_stg_ocbchannel_authen_type
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
  - name: v_stg_ocbchannel_branch_mapping
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
  - name: v_stg_ocbchannel_customer_file
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
  - name: v_stg_ocbchannel_eb_package_type
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
  - name: v_stg_ocbchannel_eb_user_channel
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key
  - name: v_stg_ocbchannel_eb_user_session
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key
  - name: v_stg_ocbchannel_eb_users
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
  - name: v_stg_ocbchannel_ocb_bill_providers
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
  - name: v_stg_ocbchannel_ocb_bill_service_provider
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key
  - name: v_stg_ocbchannel_ocb_bill_services
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
  - name: v_stg_ocbchannel_soa_apportion_detail
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
  - name: v_stg_ocbchannel_soa_cust_account
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
  - name: v_stg_ocbchannel_soa_cust_ft
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
  - name: v_stg_ocbchannel_soa_cust_package
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
  - name: v_stg_ocbchannel_soa_cust_termdeposit
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
  - name: v_stg_ocbchannel_soa_ft_batch_details
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key
  - name: v_stg_ocbchannel_soa_hdtg_da_nang
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
  - name: v_stg_ocbchannel_soa_multi_crdr_trans
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
  - name: v_stg_ocbchannel_soa_ntdt_register
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
  - name: v_stg_ocbchannel_soa_ntdt_register_detail
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key
  - name: v_stg_ocbchannel_soa_thq_register
    columns:
      - name: hashkey
        tests:
          - unique:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
  - name: v_stg_ocbchannel_trans_type_mapping
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key
  - name: v_stg_ocbchannel_user_login_log
    columns:
      - name: hashkey
        tests:
          - not_null:
              config:
                where: "source_event_date = to_date('{{ var(\"target_date\") }}', 'yyyyMMdd')"
    data_tests:
      - unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hashkey
              - ma_key
